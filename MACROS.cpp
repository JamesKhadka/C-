#include <iostream>
using namespace std;
#define mul(a, b) a *b
#define div(a, b) a / b
int main()
{
    int x = 2, y = 3;
    float n = 2, m = 2;
    cout << "the multiplication is=" << mul(x, y) << endl;
    cout << "the division is=" << div(n, m);
}